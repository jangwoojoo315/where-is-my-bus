// 이 파일을 복사해서 config.js 로 만든 뒤, 본인의 키를 넣으세요.
//   cp config.example.js config.js
// 공공데이터포털(data.go.kr)에서 발급받은 "일반 인증키(Decoding)"를 입력합니다.
// (config.js 는 .gitignore 로 git 에 올라가지 않습니다)
export const API_KEY = "";
